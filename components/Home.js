import React from 'react';
import { SafeAreaView, Text } from 'react-native';

const Home = () => {
    return (
        <SafeAreaView>
            <Text>Wlecome to Home page</Text>
        </SafeAreaView>
    );
};

export default Home;